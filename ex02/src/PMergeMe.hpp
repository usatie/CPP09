#ifndef PMERGEME_HPP
# define PMERGEME_HPP

class PMergeMe {
public:
	PMergeMe() ;
	PMergeMe(const PMergeMe& p) ;
	PMergeMe& operator =(const PMergeMe& p) ;
	~PMergeMe() ;
protected:
private:
} ;

#endif
